Field — wraps any control with a label, optional/required marker, and helper hint text. Pair with Input, Textarea, Select.

```jsx
<Field label="GitHub URL" htmlFor="gh" optional hint="Public repositories only.">
  <Input id="gh" placeholder="https://github.com/username" />
</Field>
```

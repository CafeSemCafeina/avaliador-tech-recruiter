Tag — mono pill chip for a tech-stack or skill. `primary` visually distinguishes one of the (max 3) primary stacks that guide the analysis; `removable` adds an × button.

```jsx
<Tag>PostgreSQL</Tag>
<Tag primary removable onRemove={fn}>React</Tag>
```

Textarea — multi-line input for the job description, pasted resume/LinkedIn text, and recruiter notes. Set `showCount` + `maxLength` for a live counter.

```jsx
<Textarea rows={8} placeholder="Paste the job description…" showCount maxLength={5000} />
```

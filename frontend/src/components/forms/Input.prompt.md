Input — single-line text/url/number field with a calm-blue focus ring; optional `leading` icon adornment.

```jsx
<Input placeholder="https://github.com/username" leading={icon} />
```
Use `invalid` for error state, `size` sm/md/lg.

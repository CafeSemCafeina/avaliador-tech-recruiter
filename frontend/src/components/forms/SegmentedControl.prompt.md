SegmentedControl — compact inline single-select; the canonical control for the seniority baseline.

```jsx
<SegmentedControl
  options={["Intern", "Junior", "Mid-level", "Senior", "Staff"]}
  value={level}
  onChange={setLevel}
/>
```

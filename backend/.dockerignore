# Keep secrets and local cruft out of the build context / image.
.env
.env.*
*.log
*.md
Dockerfile
.dockerignore

# Never bake GCP service-account keys into the image; mount them at runtime.
*.json.key
gcp-sa*.json
service-account*.json

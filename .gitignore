# OS
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/

# Env and secrets
.env
.env.*
!.env.example

# Node
node_modules/
dist/
coverage/

# Go
bin/
*.test
*.out

# Python/doc parsing
.venv/
__pycache__/
*.pyc

# Temporary uploads/reports
tmp/
uploads/
reports/

# Archives (design exports are unpacked into the repo, not committed as zips)
*.zip

# Orchestration: agent worktrees live inside the repo, gitignored (ADR-0016)
.worktrees/
# per-worktree agent work order (assembled, not source)
.agent-task.md

